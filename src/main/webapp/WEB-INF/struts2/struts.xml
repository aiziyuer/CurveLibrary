<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
        "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>

    <!--刷新国际化资源-->
    <constant name="struts.i18n.encoding" value="UTF-8"/>
    <constant name="struts.i18n.reload" value="true"/>


    <!--出错时会提示更详细的错误信息-->
    <constant name="struts.enable.DynamicMethodInvocation" value="true" />

    <!--开发阶段配置，在web服务器出错时会尽量打印出来，一样也是生产阶段设置为false，避免后台结构被人发现。-->
    <constant name="struts.configuration.xml.reload" value="true"/>

    <!-- caution: in release version, this value must be set false -->
    <constant name="struts.devMode" value="true"/>

    <package name="default" namespace="/" extends="struts-default">
        <result-types>
            <result-type name="tiles" class="org.apache.struts2.views.tiles.TilesResult" />
        </result-types>

        <action name="login"
                class="com.aiziyuer.webapp.login.action.LoginAction" >
            <result name="success" type="tiles">com.aiziyuer.webapp.login.action.LoginAction</result>
        </action>
    </package>


</struts>